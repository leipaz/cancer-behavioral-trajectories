# Models

This folder is intended for **saved models and checkpoints** produced by the analysis pipelines (e.g., LDA models, dictionaries, embeddings).

Large binaries may be excluded from version control depending on the target release policy.

