# Notebooks

The main analysis pipeline lives in **[`vq-vae_lda_pipeline/`](vq-vae_lda_pipeline/)** (5 notebooks, run in order).

Add collaborator-specific notebooks in separate subfolders under `notebooks/` as needed.
